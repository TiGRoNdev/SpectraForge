# 🔧 ПЛАН ДЕКОМПОЗИЦИИ P0.2: HybridFreGSRenderer

**Дата**: 2025-10-08  
**Задача**: P0.2 из REFACTORING_PRIORITY_MATRIX.md  
**Цель**: Декомпозиция HybridFreGSRenderer.cpp (1382 строки → ~250 строк orchestrator)  
**Статус**: 🔄 В ПРОЦЕССЕ

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

```yaml
Файл: src/rendering/HybridFreGSRenderer.cpp
Строк: 1382 строки
Лимит: 500 строк
Превышение: 276%
Приоритет: P0 - КРИТИЧЕСКИЙ
```

### Множественные ответственности:

1. **Vulkan Initialization** (~200 строк)
   - `createInstance()` - создание Vulkan instance
   - `pickPhysicalDevice()` - выбор GPU
   - `createLogicalDevice()` - создание logical device
   - `createAllocator()` - VMA allocator

2. **Swapchain Management** (~180 строк)
   - `createSurfaceX11()` - создание surface
   - `createSwapchainAndViews()` - swapchain + image views
   - `destroySwapchainAndViews()` - очистка

3. **Frame Synchronization** (~150 строк)
   - `beginFrame()` - начало кадра
   - `endFrame()` - завершение кадра
   - `createSyncObjects()` - семафоры и fences

4. **Render Pass Coordination** (~300 строк)
   - `createRenderPass()` - render pass
   - `createFramebuffers()` - framebuffers
   - `recordCommandBuffer()` - запись команд
   - `renderFrame()` - координация рендеринга

5. **Command Buffer Management** (~100 строк)
   - `createCommandPool()` - command pool
   - `createCommandBuffers()` - command buffers

6. **Debug Functionality** (~150 строк)
   - `setDebugMode()` - режимы отладки
   - `enableWireframe()` - wireframe
   - `setBackgroundColor()` - фон
   - `saveScreenshot()` - скриншоты

7. **Statistics Collection** (~100 строк)
   - `getDetailedStats()` - детальная статистика
   - `getGPUInfo()` - информация о GPU
   - `getStats()` - общая статистика

---

## 🎯 ЦЕЛЕВАЯ АРХИТЕКТУРА

### Новые классы (7):

```
┌────────────────────────────────────────────────────┐
│          HybridFreGSRenderer (Orchestrator)        │
│                    ~250 строк                      │
├────────────────────────────────────────────────────┤
│ Координирует все подсистемы                        │
│ Реализует IRenderer interface                      │
└────────────────────────────────────────────────────┘
                      ▲
                      │ uses
    ┌─────────────────┼─────────────────┐
    │                 │                 │
┌───┴────┐  ┌────────┴────────┐  ┌────┴─────┐
│ Core   │  │   Swapchain     │  │  Frame   │
│Manager │  │    Manager      │  │ Manager  │
│~200 л  │  │    ~150 л       │  │  ~180 л  │
└────────┘  └─────────────────┘  └──────────┘
    
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│Pipeline  │  │  Debug   │  │  Stats   │  │CommandBuf│
│ Manager  │  │  Manager │  │ Manager  │  │ Manager  │
│  ~200 л  │  │  ~100 л  │  │   ~80 л  │  │  ~100 л  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
```

---

## 📝 ДЕТАЛЬНЫЙ ПЛАН КЛАССОВ

### Класс 1: RendererCore (~200 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/RendererCore.h`
- `src/rendering/core/RendererCore.cpp`

**Ответственность**: Инициализация Vulkan core (instance, device, allocator)

**Методы**:
```cpp
class RendererCore {
public:
    bool initialize();
    void shutdown();
    
    vk::Instance getInstance() const;
    vk::PhysicalDevice getPhysicalDevice() const;
    vk::Device getDevice() const;
    vk::Queue getGraphicsQueue() const;
    vk::Queue getPresentQueue() const;
    VmaAllocator getAllocator() const;
    uint32_t getGraphicsQueueFamily() const;
    uint32_t getPresentQueueFamily() const;
    
private:
    bool createInstance();
    bool pickPhysicalDevice();
    bool createLogicalDevice(vk::SurfaceKHR surface);
    bool createAllocator();
    
    vk::Instance instance_{};
    vk::PhysicalDevice physicalDevice_{};
    vk::Device device_{};
    vk::Queue graphicsQueue_{};
    vk::Queue presentQueue_{};
    VmaAllocator allocator_ = VK_NULL_HANDLE;
    uint32_t graphicsQueueFamily_ = 0;
    uint32_t presentQueueFamily_ = 0;
};
```

**Тесты** (`tests/renderer_core_test.cpp`):
- `RendererCoreTest.InitializeSuccess`
- `RendererCoreTest.CreateInstanceSuccess`
- `RendererCoreTest.PickPhysicalDeviceSuccess`
- `RendererCoreTest.CreateLogicalDeviceSuccess`
- `RendererCoreTest.CreateAllocatorSuccess`
- `RendererCoreTest.ShutdownCleanup`

---

### Класс 2: SwapchainManager (~150 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/SwapchainManager.h`
- `src/rendering/core/SwapchainManager.cpp`

**Ответственность**: Управление swapchain и surface

**Методы**:
```cpp
class SwapchainManager {
public:
    SwapchainManager(vk::Instance instance, vk::PhysicalDevice physicalDevice, 
                     vk::Device device);
    
    bool createSurfaceX11(void* x11Display, void* x11Window);
    bool createSwapchain(uint32_t width, uint32_t height);
    void destroySwapchain();
    void shutdown();
    
    vk::SurfaceKHR getSurface() const;
    vk::SwapchainKHR getSwapchain() const;
    const std::vector<vk::Image>& getSwapchainImages() const;
    const std::vector<vk::ImageView>& getSwapchainImageViews() const;
    vk::Format getSwapchainFormat() const;
    vk::Extent2D getSwapchainExtent() const;
    
private:
    bool createSwapchainAndViews(uint32_t width, uint32_t height);
    
    vk::Instance instance_;
    vk::PhysicalDevice physicalDevice_;
    vk::Device device_;
    
    vk::SurfaceKHR surface_{};
    vk::SwapchainKHR swapchain_{};
    std::vector<vk::Image> swapchainImages_{};
    std::vector<vk::ImageView> swapchainImageViews_{};
    vk::Format swapchainFormat_ = vk::Format::eB8G8R8A8Unorm;
    vk::Extent2D swapchainExtent_{1920, 1080};
};
```

**Тесты** (`tests/swapchain_manager_test.cpp`):
- `SwapchainManagerTest.CreateSurfaceX11Success`
- `SwapchainManagerTest.CreateSwapchainSuccess`
- `SwapchainManagerTest.DestroySwapchain`
- `SwapchainManagerTest.GetSwapchainImages`

---

### Класс 3: FrameManager (~180 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/FrameManager.h`
- `src/rendering/core/FrameManager.cpp`

**Ответственность**: Управление кадрами и синхронизацией

**Методы**:
```cpp
class FrameManager {
public:
    FrameManager(vk::Device device);
    
    bool initialize();
    void shutdown();
    
    void beginFrame(vk::SwapchainKHR swapchain);
    void endFrame(vk::SwapchainKHR swapchain, vk::Queue presentQueue, 
                  vk::CommandBuffer commandBuffer);
    
    uint32_t getCurrentImageIndex() const;
    size_t getCurrentFrame() const;
    vk::Semaphore getImageAvailableSemaphore() const;
    vk::Semaphore getRenderFinishedSemaphore() const;
    vk::Fence getInFlightFence() const;
    
    bool isDeviceLost() const;
    
private:
    bool createSyncObjects();
    
    vk::Device device_;
    
    std::vector<vk::Semaphore> imageAvailableSemaphores_{};
    std::vector<vk::Semaphore> renderFinishedSemaphores_{};
    std::vector<vk::Fence> inFlightFences_{};
    
    static constexpr int MAX_FRAMES_IN_FLIGHT = 2;
    size_t currentFrame_ = 0;
    uint32_t currentImageIndex_ = 0;
    bool deviceLost_ = false;
};
```

**Тесты** (`tests/frame_manager_test.cpp`):
- `FrameManagerTest.InitializeSuccess`
- `FrameManagerTest.CreateSyncObjectsSuccess`
- `FrameManagerTest.BeginFrameSuccess`
- `FrameManagerTest.EndFrameSuccess`
- `FrameManagerTest.DeviceLostHandling`

---

### Класс 4: PipelineManager (~200 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/PipelineManager.h`
- `src/rendering/core/PipelineManager.cpp`

**Ответственность**: Управление render pass, framebuffers, command buffers

**Методы**:
```cpp
class PipelineManager {
public:
    PipelineManager(vk::Device device);
    
    bool initialize(const std::vector<vk::ImageView>& swapchainImageViews,
                    vk::Format swapchainFormat, vk::Extent2D swapchainExtent);
    void shutdown();
    
    bool createRenderPass(vk::Format swapchainFormat);
    bool createFramebuffers(const std::vector<vk::ImageView>& swapchainImageViews,
                           vk::Extent2D swapchainExtent);
    bool createCommandPool(uint32_t graphicsQueueFamily);
    bool createCommandBuffers();
    
    vk::RenderPass getRenderPass() const;
    const std::vector<vk::Framebuffer>& getFramebuffers() const;
    vk::CommandPool getCommandPool() const;
    const std::vector<vk::CommandBuffer>& getCommandBuffers() const;
    
    void recordCommandBuffer(vk::CommandBuffer commandBuffer, uint32_t imageIndex,
                            const std::function<void(vk::CommandBuffer)>& recordFunc);
    
private:
    vk::Device device_;
    
    vk::RenderPass renderPass_{};
    std::vector<vk::Framebuffer> framebuffers_{};
    vk::CommandPool commandPool_{};
    std::vector<vk::CommandBuffer> commandBuffers_{};
    
    static constexpr int MAX_FRAMES_IN_FLIGHT = 2;
};
```

**Тесты** (`tests/pipeline_manager_test.cpp`):
- `PipelineManagerTest.CreateRenderPassSuccess`
- `PipelineManagerTest.CreateFramebuffersSuccess`
- `PipelineManagerTest.CreateCommandPoolSuccess`
- `PipelineManagerTest.CreateCommandBuffersSuccess`
- `PipelineManagerTest.RecordCommandBuffer`

---

### Класс 5: RendererDebugger (~100 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/RendererDebugger.h`
- `src/rendering/core/RendererDebugger.cpp`

**Ответственность**: Debug функционал и настройки

**Методы**:
```cpp
class RendererDebugger {
public:
    void setDebugMode(int mode);
    int getDebugMode() const;
    
    void enableWireframe(bool enable);
    bool isWireframeEnabled() const;
    
    void enableBackfaceCulling(bool enable);
    bool isBackfaceCullingEnabled() const;
    
    void enableDepthTest(bool enable);
    bool isDepthTestEnabled() const;
    
    void enableAlphaBlending(bool enable);
    bool isAlphaBlendingEnabled() const;
    
    void enableEarlyTermination(bool enable);
    bool isEarlyTerminationEnabled() const;
    
    void setBackgroundColor(float r, float g, float b, float a);
    glm::vec4 getBackgroundColor() const;
    
    void setViewport(int x, int y, int width, int height);
    vk::Viewport getViewport() const;
    
    void setDebugCallback(std::function<void(const std::string&)> callback);
    void log(const std::string& message) const;
    
private:
    int currentDebugMode_ = 0;
    glm::vec4 backgroundColor_{0.1f, 0.2f, 0.3f, 1.0f};
    bool wireframeEnabled_ = false;
    bool depthTestEnabled_ = true;
    bool backfaceCullingEnabled_ = true;
    bool alphaBlendingEnabled_ = true;
    bool earlyTerminationEnabled_ = true;
    vk::Viewport currentViewport_;
    std::function<void(const std::string&)> debugCallback_;
};
```

**Тесты** (`tests/renderer_debugger_test.cpp`):
- `RendererDebuggerTest.SetDebugMode`
- `RendererDebuggerTest.EnableWireframe`
- `RendererDebuggerTest.SetBackgroundColor`
- `RendererDebuggerTest.SetViewport`
- `RendererDebuggerTest.DebugCallback`

---

### Класс 6: RendererStatistics (~80 строк)

**Файлы**:
- `include/SpectraForge/Rendering/Core/RendererStatistics.h`
- `src/rendering/core/RendererStatistics.cpp`

**Ответственность**: Сбор и агрегация статистики

**Методы**:
```cpp
class RendererStatistics {
public:
    RendererStatistics(vk::PhysicalDevice physicalDevice);
    
    void updateStats(const RenderingStats& stats);
    
    RenderingStats getStats() const;
    DetailedRenderingStats getDetailedStats() const;
    GPUInfo getGPUInfo() const;
    
    void setTriangleBudget(uint32_t maxTriangles);
    uint32_t getTriangleBudget() const;
    
private:
    vk::PhysicalDevice physicalDevice_;
    RenderingStats stats_{};
    uint32_t triangleBudget_ = 100000;
};
```

**Тесты** (`tests/renderer_statistics_test.cpp`):
- `RendererStatisticsTest.UpdateStats`
- `RendererStatisticsTest.GetStats`
- `RendererStatisticsTest.GetDetailedStats`
- `RendererStatisticsTest.GetGPUInfo`
- `RendererStatisticsTest.SetTriangleBudget`

---

### Класс 7: HybridFreGSRenderer (Orchestrator) (~250 строк)

**Файлы**:
- `include/SpectraForge/Rendering/HybridFreGSRenderer.h` (обновить)
- `src/rendering/HybridFreGSRenderer.cpp` (рефакторинг)

**Ответственность**: Координация всех подсистем

**Методы** (упрощенные):
```cpp
class HybridFreGSRenderer final : public IRenderer {
public:
    HybridFreGSRenderer();
    ~HybridFreGSRenderer() override;
    
    // IRenderer interface
    bool initialize() override;
    bool attachWindow(void* x11Display, void* x11Window, uint32_t width, uint32_t height);
    void renderFrame(const FrameData& frameData) override;
    void shutdown() override;
    void beginFrame() override;
    void endFrame() override;
    
    // IRenderer getters
    RendererType getType() const override;
    bool supportsFeature(RenderingFeature feature) const override;
    std::string getName() const override;
    std::string getApiVersion() const override;
    bool isReady() const override;
    bool isInitialized() const override;
    RenderingStats getStats() const override;
    
    // Data upload
    void uploadGaussians(const std::vector<GaussianSplat>& gaussians);
    void uploadTriangles(const std::vector<Triangle>& triangles);
    void uploadMesh(const std::shared_ptr<Mesh3D>& mesh, const Math::Matrix4& transform);
    void setCamera(Camera3D* camera);
    
    // Debug API (delegates to RendererDebugger)
    void setDebugMode(int mode) override;
    int getDebugMode() const override;
    void enableWireframe(bool enable) override;
    void enableBackfaceCulling(bool enable) override;
    void enableDepthTest(bool enable) override;
    void setBackgroundColor(float r, float g, float b, float a) override;
    glm::vec4 getBackgroundColor() const override;
    void setViewport(int x, int y, int width, int height) override;
    void enableAlphaBlending(bool enable) override;
    void setTriangleBudget(uint32_t maxTriangles) override;
    void enableEarlyTermination(bool enable) override;
    DetailedRenderingStats getDetailedStats() const override;
    bool saveScreenshot(const std::string& filename) const override;
    std::vector<uint8_t> getFramebufferData() const override;
    void setDebugCallback(std::function<void(const std::string&)> callback) override;
    void flushUniforms() override;
    GPUInfo getGPUInfo() const override;
    
    // Accessors
    TriangleSplattingPass* getTriangleSplattingPass();
    void setRenderMode(RenderMode mode);
    bool isDeviceLost() const;
    
private:
    bool initialized_ = false;
    RenderMode renderMode_ = RenderMode::TriangleSplatting;
    Camera3D* camera_ = nullptr;
    
    // Dependency-injected components (БУДЕТ ЗАМЕНЕНО НА DI КОНТЕЙНЕР В P0.6)
    std::unique_ptr<RendererCore> core_;
    std::unique_ptr<SwapchainManager> swapchain_;
    std::unique_ptr<FrameManager> frame_;
    std::unique_ptr<PipelineManager> pipeline_;
    std::unique_ptr<RendererDebugger> debugger_;
    std::unique_ptr<RendererStatistics> statistics_;
    
    // Rendering passes
    std::unique_ptr<TriangleSplattingPass> triangleSplattingPass_;
    std::unique_ptr<FreGSPass> fregsPass_;
    
    // Helpers
    bool initializeTriangleSplatting();
    void recordCommandBuffer(vk::CommandBuffer commandBuffer, uint32_t imageIndex);
    std::vector<Triangle> convertMeshToTriangles(const Mesh3D& mesh, const Math::Matrix4& transform);
    glm::mat4 getCorrectedViewProjMatrix() const;
};
```

**Тесты** (`tests/hybrid_fregs_renderer_decomposition_test.cpp`):
- `HybridFreGSRendererTest.InitializeSuccess`
- `HybridFreGSRendererTest.AttachWindowSuccess`
- `HybridFreGSRendererTest.RenderFrameSuccess`
- `HybridFreGSRendererTest.DebugAPIIntegration`
- `HybridFreGSRendererTest.StatisticsIntegration`
- `HybridFreGSRendererTest.ComponentsInitialized`

---

## 🧪 TDD СТРАТЕГИЯ

### Фаза 1: RED (Дни 1-2) - Написание тестов

1. ✅ **Создать тестовые файлы**:
   - `tests/renderer_core_test.cpp`
   - `tests/swapchain_manager_test.cpp`
   - `tests/frame_manager_test.cpp`
   - `tests/pipeline_manager_test.cpp`
   - `tests/renderer_debugger_test.cpp`
   - `tests/renderer_statistics_test.cpp`
   - `tests/hybrid_fregs_renderer_decomposition_test.cpp`

2. ✅ **Написать ~50 тестов** (7 файлов × ~7-10 тестов каждый)

3. ✅ **Все тесты FAIL** (RED phase)

### Фаза 2: GREEN (Дни 3-5) - Реализация классов

1. ✅ Реализовать RendererCore
2. ✅ Реализовать SwapchainManager
3. ✅ Реализовать FrameManager
4. ✅ Реализовать PipelineManager
5. ✅ Реализовать RendererDebugger
6. ✅ Реализовать RendererStatistics
7. ✅ Рефакторинг HybridFreGSRenderer (orchestrator)

**Цель**: Все тесты GREEN

### Фаза 3: REFACTOR (Дни 6-7) - Оптимизация и интеграция

1. ✅ Оптимизация кода
2. ✅ Добавление Doxygen комментариев
3. ✅ Интеграция с DI (если P0.6 уже выполнен)
4. ✅ Performance benchmark
5. ✅ Финальная валидация

---

## 📊 МЕТРИКИ УСПЕХА

```yaml
До декомпозиции:
  Файлов: 2 (header + impl)
  Строк: 1382 строки
  Классов: 1 God Class
  SRP нарушений: КАТАСТРОФИЧЕСКОЕ
  Тестируемость: НИЗКАЯ

После декомпозиции:
  Файлов: 16 (8 headers + 8 impl)
  Строк (impl): ~1160 строк (распределено по 8 классам)
  Классов: 8 (7 компонентов + 1 orchestrator)
  SRP нарушений: 0 ✅
  Тестируемость: ВЫСОКАЯ ✅
  Test Coverage: 100% (50+ тестов) ✅
```

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

### Риск 1: Performance Regression
**Вероятность**: 30%  
**Митигация**: Benchmark после каждого шага

### Риск 2: Breaking Changes в API
**Вероятность**: 40%  
**Митигация**: Сохранить public API, изменить только private impl

### Риск 3: Тесты не проходят
**Вероятность**: 60%  
**Митигация**: Mock Vulkan dependencies через GMock

---

## ✅ CHECKLIST

### Pre-Task:
- [ ] Создать файлы для 7 новых классов
- [ ] Написать 50+ unit тестов (TDD RED)
- [ ] Baseline benchmark текущего HybridFreGSRenderer

### During Task:
- [ ] Реализовать RendererCore
- [ ] Реализовать SwapchainManager
- [ ] Реализовать FrameManager
- [ ] Реализовать PipelineManager
- [ ] Реализовать RendererDebugger
- [ ] Реализовать RendererStatistics
- [ ] Рефакторинг HybridFreGSRenderer orchestrator
- [ ] Все тесты GREEN ✅

### Post-Task:
- [ ] Test Coverage 100%
- [ ] Performance ±5% от baseline
- [ ] Doxygen comments везде
- [ ] Обновить SOLID_VIOLATIONS_LIST.md
- [ ] Обновить REFACTORING_PRIORITY_MATRIX.md

---

## 📈 ПРОГРЕСС

```
День 1: [██░░░░░░░░] 20% - Написание тестов (TDD RED)
День 2: [████░░░░░░] 40% - Завершение тестов
День 3: [██████░░░░] 60% - Реализация Core/Swapchain/Frame
День 4: [████████░░] 80% - Реализация Pipeline/Debug/Stats
День 5: [█████████░] 90% - Рефакторинг Orchestrator
День 6-7: [██████████] 100% - Оптимизация, документация, валидация
```

---

**Статус**: ✅ ПЛАН ГОТОВ, НАЧИНАЕМ ВЫПОЛНЕНИЕ  
**Следующий шаг**: Создание структуры файлов и написание тестов (TDD RED)
