#version 450

// Depth prepass фрагментный шейдер - просто записывает глубину
// Никаких выходных данных не нужно, только запись в depth buffer

void main() {
    // Фрагментный шейдер для depth prepass ничего не делает
    // Глубина автоматически записывается в depth buffer
}
